<name="main", function="
= #str[0] 97;
? if == #str[0] 97;
	! write 'we are in the if\n';
//	? if == 1 1;
//		! write 'we are in a neasted if\n';
//	?endif;
	! write 'still in it\n';
# elseif && == #str[0] 97 == 1 1;
	! write 'we are in the elseif\n';
# else;
	! write 'we are in the else\n';
?endif;
= $i ! facto ( 5 );
! write $i '\n';
= $nb #str;
= $nb[0] 104;
! my_putstr ( $nb );
! write '\n';
! return 1;
", nb="2">
